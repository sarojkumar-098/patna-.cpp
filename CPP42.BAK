#include <stdio.h>
#include <conio.h>
void main()
{
float area(int,int);
float a;
int b,h;
scanf("%d%d",&b,&h);
a=area(b,h);
printf("a=%f\n",a);
getch();
}
float area(int m,int n)
{
float t;
t=0.5*m*n;
return(t);
}